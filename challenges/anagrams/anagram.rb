# same word is not an anagram
# case doesn't matter
# return empty list if no anagrams

class Anagram
  def initialize(anagram)
    @anagram = anagram
  end

  def match(array)
    matches = []

    matches
  end

  private

  attr_reader :anagram
end
